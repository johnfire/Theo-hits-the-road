# Database Configuration
DATABASE_URL=postgresql://artcrm_admindude:aw4e0rfeA1!Q@localhost:5432/artcrm

# Timezone
TIMEZONE=Europe/Berlin

# AI Configuration
# DeepSeek (routine tasks: scoring, briefs, suggestions)
DEEPSEEK_API_KEY=your-deepseek-api-key-here
DEEPSEEK_BASE_URL=https://api.deepseek.com
# Default model for AI commands: deepseek-chat | deepseek-reasoner | claude
DEFAULT_AI_MODEL=deepseek-chat

# Claude API (high-quality writing: gallery letters, proposals)
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# Lead Generation (Phase 6-Alpha)
# Google Maps API - Get key at: https://console.cloud.google.com/apis/credentials
# Free tier: $200/month credit (roughly 28,000 requests)
GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here
LEAD_SCOUT_BATCH_SIZE=20
LEAD_SCOUT_RATE_LIMIT_SECONDS=1.0

# Email Configuration (Phase 7)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your-email@example.com
SMTP_PASSWORD=your-password
IMAP_HOST=imap.example.com
IMAP_PORT=993

# Application Settings
FOLLOW_UP_CADENCE_MONTHS=4
DORMANT_THRESHOLD_MONTHS=12

# Logging
# Controls verbosity of logs/artcrm.log
# DEBUG   — entry/exit for every function call (noisy, use when diagnosing a bug)
# INFO    — command OK/FAIL lines only (default, good for daily use)
# WARNING — only warnings and errors
# ERROR   — failures only
LOG_LEVEL=INFO
